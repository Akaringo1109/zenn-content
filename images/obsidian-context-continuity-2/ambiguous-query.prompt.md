---
generated_at: "2026-07-14 04:36:43"
model: "gemini-3.1-flash-lite-image"
---

## 入力プロンプト（内容部分）

（重要な描画ルール・厳守：この図解にタイトル・上部見出し帯・リード文・サブタイトルを一切描かないこと。最初の区画見出しをページ上部のタイトルに昇格させず、すべての区画を対等なブロックとして扱う。画像上端から余白を作らず、下記の区画ブロックだけで構成し、図解本体をキャンバス全面に最大化する。この括弧内の注意書き自体は画像に描かない）
1つの曖昧な質問が、3つの異なる方法で処理され、最後に1つの回答へ合流する様子。

## 曖昧な質問
- 先月のあの件、あった気がする

## 3つの合わせ技
- 意味検索：意味の近いノートを引く
- git網羅：変更と追加を漏れなく列挙
- 推論：口約束まで拾う

## 回答
- 経緯込みで返す

---

## API送信プロンプト（合成後・全文）

あなたはプロのインフォグラフィック・デザイナーです。プレゼン資料やドキュメントのスライドに
そのまま貼れる、清書された高品質な図解を1枚作成してください。あくまで「スライドの中でこの情報を
どう表現するか」という観点で構成すること。

【スタイル】
- フラットで清潔なベクター表現を基本とする（立体・陰影・光沢は付けない）。表現形式（図・アイコン・チャート・ダイアグラム等）は内容に最も合うものを自由に選ぶ。
- グラデーション・ドロップシャドウ・3D効果・発光表現は使わない。装飾より構造・可読性を優先する。
- 配色は落ち着いたプロフェッショナルなトーンで統一。主役1色・基調グレー・アクセント1色までとし、具体的なカラーコードは別途の配色仕様に従う。
- アクセント色は紙面の3%以下に抑え、強調は1〜2点に限定する。
- 全体は 16:9 の横長。背景色・主要色は別途指定される配色仕様（カラーコード）に厳密に従う。

【情報密度（重要）】
- 情報を詰め込まない。本文は3〜5ブロック程度に圧縮し、1ブロックの本文は最大3行程度にする。
- 元情報が多い場合は文字量を40〜60%程度まで削る（意味の骨子は維持）。長文は要点だけにし、同義項目は統合、数値は代表値だけ残す。
- 文章で説明しきろうとせず、構造・ラベル・主要数値で伝える。
- 強弱を明確に：重要な情報は大きく、補足は小さく（文字サイズのジャンプ率は最大7倍以内）。
- 読めない小さな文字は作らない。文字が小さくなるなら情報を削る。日本語が崩れそうなら短く言い換える。

【文字サイズ・階層】
- 文字サイズで階層を明確につける（目安・相当値）：メインタイトル 42〜52pt／メッセージライン（リード）20〜26pt／セクション見出し 18〜22pt／主要数値・キーメッセージ 22〜30pt／本文 11〜14pt／補助ラベル・注釈 10〜12pt。※メインタイトル・メッセージラインは「スライド用途で内容にタイトル／リードが含まれる場合」のみ使う。
- 最小でも 10pt相当以上。読めない小さな文字は作らない。重要テキストを小さくせず、不要な情報を削ってサイズを確保する。

【言語・テキスト・フォント】
- 図中の文字はすべて自然で正確な「日本語」で書く（英語に置き換えない）。不自然な改行・誤字を避ける。
- フォントは配色仕様で指定した読みやすいサンセリフで全体を統一する（明朝体・手書き風・装飾フォント・過度なイタリックは使わない）。
- タイトル・見出し・重要語は太字、本文は標準〜中太（過度に太くしない）。
- ラベルは簡潔に。行間は広めに、文字間は詰めすぎない。

【描画領域（キャンバス最大化・最優先）】
- 図解本体はキャンバスいっぱいに大きく描く。左右・上下の外周マージンは最小限にし、図解を中央に小さくまとめない。
- 各ブロック・カード・図要素は、可読性を保ったまま可能な限り大きくスケールさせ、画面の縦横を余さず使う（内部の要素間ホワイトスペースは可読性のため確保しつつ、外周に大きな空きを残さない）。
- タイトル／リードが無いとき（後述の図解・インフォグラフィック単体モード）は、上部にタイトル用の帯・余白を設けず、その領域も図解本体に充てて全面に展開する。

【タイトル／リードの有無（モード切替・重要）】
- タイトル見出し・リード（メッセージライン）は、可視化する内容にそれらが明示的に含まれている場合のみ描く（スライド用途）。
- 含まれる場合：タイトルを左上に大きく1行（長い場合のみ2行）、直下に1〜2行のリードを置き、階層を明確にする。
- 含まれない場合（図解・インフォグラフィック単体）：タイトルやリードを勝手に作らない。上部見出し帯を置かず、紙面全体を図解本体に使う。

【レイアウト＝自由演技（重要・スライド／図解 共通）】
- レイアウトの「型」は画像生成側の解釈に委ねる。均一なグリッドや同サイズカードの機械的並置に固定しない。列数・ブロックの大小・形・配置は内容に合わせて自由に変えてよい。
- 内容の関係性（時間の流れ・階層・対比・因果・比重）に最も合う構図を自分で選ぶ。重要な要素は大きく主役に、補足は小さく、強弱と変化をつける。
- フロー・ゾーニング・大小ブロック混在・左右対比・中心放射など、決まった雛形に押し込めず、内容に応じた表現を選ぶ。
- 守るのは次だけ：各ブロックに見出しを付け、要素同士の関係が一目で伝わること。装飾は最小限（影・立体・グラデーション・発光・過剰な塗りは付けない＝フラット）。罫線・余白・アイコンは構造理解を助ける範囲で自由に使ってよい（アイコンを主役にしない／人物・ロボットは避ける）。

【入力（可視化する内容）の読み方】
- 「可視化する内容」は Markdown 構造（見出し・ネストしたリスト）で与えられる。見出しは大区画／グループ、ネストの深さは階層・包含・順序を表す。
- この構造を図解のブロック分け・階層・グルーピングに反映する。
- ただし # や - などの記法記号は画像内にそのまま描かず、レイアウトの手がかりとしてのみ使う。

【配色・出力仕様（カラーコードは厳密に守る）】
- 背景は必ず #FFFFFF（純白）で塗る。他の色を背景に使わない。
- 使用色: 主役色 #3B5BDB / 基調グレー #868E96 / アクセント #F08C00（アクセントは紙面の3%以下・強調は1〜2点）。トーンを統一する。
- 上記の指定パレット以外の色を新たに使わない（特に原色を増やして紙面を賑やかにしない）。
- 図中テキストのフォントは Noto Sans JP 系の読みやすいサンセリフで統一する。
- 出力比率は 16:9 の横長。
- ※「#」に続けて英数字が並ぶカラーコード文字列（例 #FFFFFF）は色の指定であって描画対象ではないため、画像内に文字・ラベル・凡例として一切描かないこと。

【図解タイプ】
関係性・流れ・構造そのものを主役にする。フロー図・レイヤー図・ブロック図など、
対象の構造に最も合う形式を選ぶ。矢印・包含・階層で関係を明示し、余計な装飾は足さない。
構造図はキャンバス全面に大きく展開し、ノード・ブロックを可能な限り大きく描く（外周マージンは最小限）。
タイトル／リードが内容に含まれない限り上部見出しは置かず、全面を構造の表現に充てる。

【可視化する内容（今回の指示）】
（重要な描画ルール・厳守：この図解にタイトル・上部見出し帯・リード文・サブタイトルを一切描かないこと。最初の区画見出しをページ上部のタイトルに昇格させず、すべての区画を対等なブロックとして扱う。画像上端から余白を作らず、下記の区画ブロックだけで構成し、図解本体をキャンバス全面に最大化する。この括弧内の注意書き自体は画像に描かない）
1つの曖昧な質問が、3つの異なる方法で処理され、最後に1つの回答へ合流する様子。

## 曖昧な質問
- 先月のあの件、あった気がする

## 3つの合わせ技
- 意味検索：意味の近いノートを引く
- git網羅：変更と追加を漏れなく列挙
- 推論：口約束まで拾う

## 回答
- 経緯込みで返す
